# 🎓 多智能体个性化教学系统

**个性化学习路径规划委员会 (The "Board of Education" for One)**

一个使用多智能体技术的元认知（Meta-cognitive）教学系统，采用**并行批处理 + 熔断机制**为学习者量身定制高质量学习路径。

系统不只是"推荐内容"，而是通过多智能体协作**主动修复问题**，确保每个学习节点都经过严格验证。

## 🚀 核心特性

### ⚡ 并行批处理架构

**v2.0 重大改进**：
- 🔀 **并行处理**：从串行流水线改为并行批处理，总耗时从 O(N) 降低到 O(1)
- ⏱️ **熔断机制**：单个节点处理超过 45 秒自动跳过，防止整个系统挂死
- 🛡️ **错误容错**：自动重试 + JSON 容错，绝不让系统崩坏
- 📉 **成本优化**：快速通道机制，简单节点自动通过，节省 60-80% API 调用
- 🎯 **超时控制**：45秒请求超时，防止API卡死

### 🤖 五位专家 Agent

1. **🏗️ 架构师 (Curriculum Designer)**
   - 负责设计学习大纲和知识图谱
   - 将学习目标拆解成节点和依赖关系
   - 先进行课程校准，确认用户真实水平

2. **🧠 心理学家 (Pedagogue)**
   - 评估难度是否适合学习者水平
   - 如果太难过拒绝，触发修复机制
   - 应用认知负荷理论和学习心理学原则

3. **📚 资源搜集者 (Librarian)**
   - 根据大纲检索具体的学习资料
   - 推荐视频、文章、书籍、课程等
   - 配合关键词守护防止上下文漂移

4. **🎓 用户模拟器 (Simulated Student)** ⭐ **关键创新**
   - 模拟真实学习者的当前水平
   - 试读内容，如果读不懂就报错
   - 动态跟踪已学知识（Student Profile Update）
   - 要求架构师重写，形成反馈闭环

5. **🔧 修复者 (Repair Agent)** ⭐ **质量保证核心**
   - 接收反馈并主动修复问题节点
   - 三种修复策略：修改、拆分、插入前置节点
   - 实现 Scaffolding（脚手架）机制

## 🔄 工作流程（并行批处理架构）

```
用户输入学习目标
    ↓
🎯 课程校准 (串行，必须优先执行)
    ↓
🏗️ 架构师设计学习路径（生成 N 个节点）
    ↓
⚡ 并行批处理（ThreadPoolExecutor）
    ├─ 节点 1 → 审核 → 修复(可选) → 资源 → 测试
    ├─ 节点 2 → 审核 → 修复(可选) → 资源 → 测试
    ├─ 节点 3 → 审核 → 修复(可选) → 资源 → 测试
    ├─ ...
    └─ 节点 N → 审核 → 修复(可选) → 资源 → 测试
    ↓
📊 收集结果（保持顺序）
    ↓
🎉 输出经过质量保证的学习路径

🛡️ 熔断机制：单个节点超时45秒自动跳过
🔄 错误恢复：任何错误都有兜底数据，保证系统不挂
```

## 🚀 快速开始

### 安装

```bash
# 1. 安装依赖
pip install -r requirements.txt
```

### 配置 API

创建 `.env` 文件：

```bash
# 复制配置模板
cp env.example .env

# 编辑 .env 文件，添加你的 API Key
DEEPSEEK_API_KEY=your_api_key_here
DEEPSEEK_API_BASE=https://api.deepseek.com
DEEPSEEK_CHAT_MODEL=deepseek-chat
```

### 基础使用

**Python 代码方式**：

```python
from teaching_sys import TeachingSystem

# 创建系统
system = TeachingSystem()

# 创建学习路径
result = system.create_learning_path(
    topic="量子物理",
    user_level="beginner",
    target_level="intermediate"
)

# 查看结果
print(f"总学时: {result['learning_path']['total_hours']} 小时")
print(f"节点数: {len(result['learning_path']['nodes'])}")

# 查看每个学习节点
for node in result['learning_path']['nodes']:
    print(f"- {node['title']} ({node['difficulty']})")
```

**Web UI 方式**：

```bash
# 推荐方式
python run_ui.py

# 或直接运行
streamlit run ui.py
```

⚠️ **注意**: 不要使用 `streamlit run run_ui.py`，这会导致双重启动。


## 📖 使用示例

### 示例 1：基础使用

```python
from teaching_sys import TeachingSystem

system = TeachingSystem()

result = system.create_learning_path(
    topic="Python 编程",
    user_level="beginner",
    target_level="intermediate"
)

# 查看学习路径
for i, node in enumerate(result['learning_path']['nodes'], 1):
    print(f"{i}. {node['title']} - {node['estimated_hours']}h")
```

### 示例 2：查看节点详情

```python
# 获取第一个节点的详细信息
node_id = result['learning_path']['nodes'][0]['id']
details = system.get_node_details(result, node_id)

print(f"节点: {details['node']['title']}")
print(f"资源数: {len(details['resources'])}")
print(f"心理学家反馈: {details['pedagogue_feedback']}")
print(f"学生理解度: {details['student_feedback']}")
```

### 示例 3：查看处理过程

```python
# 查看所有消息（包括修复过程）
for msg in result["messages"]:
    print(msg)

# 只看修复相关
for msg in result["messages"]:
    if any(emoji in msg for emoji in ["🔧", "➕", "✂️", "⚠️"]):
        print(msg)
```

### 示例 4：导出学习路径

```python
import json

# 导出为 JSON
with open("learning_path.json", "w", encoding="utf-8") as f:
    json.dump(result, f, ensure_ascii=False, indent=2)
```

## 🔧 高级配置

### 自定义配置

```python
from teaching_sys import TeachingSystem, DeepSeekConfig

config = DeepSeekConfig(
    api_key="your_key",
    api_base="https://api.deepseek.com",
    chat_model="deepseek-chat",
    temperature=0.8  # 更高的创造性
)

system = TeachingSystem(
    config=config,
    max_revision_iterations=3,  # 每个节点最多修订3次
    max_workers=5                # 并行线程数（默认3）
)
```

### 使用自定义 LLM

```python
from langchain_openai import ChatOpenAI
from teaching_sys import TeachingSystem

llm = ChatOpenAI(
    model="deepseek-chat",
    temperature=0.7,
    request_timeout=45,  # 45秒超时
    max_tokens=1000
)

system = TeachingSystem(llm=llm)
```

## 📊 数据结构

### LearningPath（学习路径）

```python
{
    "topic": "量子物理",
    "target_level": "intermediate",
    "total_hours": 24.5,
    "nodes": [
        {
            "id": "node_1",
            "title": "量子力学基础",
            "description": "理解量子世界的基本概念",
            "difficulty": "beginner",
            "prerequisites": [],
            "estimated_hours": 4.0,
            "key_concepts": ["波粒二象性", "不确定性原理"]
        }
    ]
}
```

### 返回结果结构

```python
result = {
    "learning_path": {...},           # 完整的学习路径
    "resources": {...},               # 每个节点的资源
    "pedagogue_feedback": [...],      # 心理学家反馈
    "student_feedback": [...],        # 学生模拟器反馈
    "messages": [...],                # 处理日志
    "completed": True                 # 是否完成
}
```

## 🎯 核心亮点

### 1. 🔄 闭环质量保证（主动修复）

系统会主动修复问题，而不是简单记录：

- ✅ **RepairAgent**: 被拒绝的节点会自动修复
- ✅ **Scaffolding**: 缺失前置知识时自动插入
- ✅ **Context Guardrails**: 检测并阻止资源跑偏
- ✅ **循环验证**: 持续修复直到通过或达到上限

**效果对比**:
- 传统方式: "❌ Too hard. Moving on anyway."
- 本系统: "❌ Too hard. 🔧 Repairing... ✅ Approved!"

### 2. ⚡ 并行批处理 + 熔断机制

- **并行处理**：使用 `ThreadPoolExecutor`，多个节点同时处理
- **熔断保护**：单个节点超时45秒自动跳过，使用兜底数据
- **错误恢复**：任何异常都有默认值，保证系统不挂死
- **快速通道**：简单节点（难度 ≤ 用户水平）自动通过，节省API调用

### 3. 🎯 课程校准（Curriculum Calibration）

不再盲目设计课程，先确认用户真实水平：

- ✅ 明确定义 "beginner" 在该主题下的含义
- ✅ 识别缺失的基础技能
- ✅ 生成全局约束防止跑偏
- ✅ 推荐必要的前置课程

### 4. 📊 动态学生档案

学生的知识不是静态的，会随学习累积：

- ✅ 每通过一个节点，档案更新
- ✅ 后续节点评估时利用已学知识
- ✅ 模拟真实的学习进程

### 5. 🛡️ 关键词守护机制

防止"HPC 课程出现 Node.js"这种离谱现象：

- ✅ 自动检测上下文漂移
- ✅ 阻止无关主题的资源
- ✅ 触发重试机制

## 🏗️ 项目结构

```
teaching_sys/
├── __init__.py          # 包入口
├── agents.py            # 五位专家 Agent 定义
├── workflow.py          # 并行批处理工作流
├── system.py            # 主系统 API
├── ui.py                # Streamlit Web UI
├── run_ui.py            # UI 启动脚本
├── cleanup.py           # 清理缓存脚本
├── requirements.txt     # 依赖列表
├── env.example          # 环境变量模板
└── README.md            # 本文档
```

## 🔬 技术栈

- **Python 3.8+**
- **LangChain**: LLM 框架
- **concurrent.futures**: 并行处理
- **DeepSeek API**: 大语言模型（或兼容的 OpenAI API）
- **Streamlit**: Web UI

## 📝 注意事项

1. **API Key**: 需要 DeepSeek API key（或兼容的 OpenAI API）
2. **后备模式**: 如果没有 API key，系统会使用启发式规则（功能有限）
3. **超时设置**: 默认45秒超时，防止API卡死
4. **并行线程**: 默认3个并发线程，可通过 `max_workers` 调整
5. **成本控制**: 快速通道机制自动跳过简单节点，节省成本

## 🐛 常见问题

### Q1: 出现超时错误

**现象**: `⚠️ Node X timed out! Using original version.`

**原因**: 单个节点处理超过45秒

**解决**: 
- 这是正常的熔断机制，系统会自动使用兜底数据
- 如果频繁超时，可以检查网络或API响应速度
- 可以调整 `request_timeout` 参数（但不要设置太长）

### Q2: 没有修复发生

**现象**: `total_repairs` 为 0

**原因**: 
- 课程设计很好，不需要修复 ✅
- 或所有节点都走了快速通道

**检查**:
```python
for msg in result["messages"]:
    if "⚡ Fast-track" in msg:
        print(msg)  # 查看哪些节点走了快速通道
```

### Q3: API Key 错误

**解决**:
1. 检查 `.env` 文件中的 API key 是否正确
2. 确保没有多余的空格或引号
3. 验证 API key 是否有效且有余额

### Q4: 运行很慢

**优化建议**:
1. 确保使用并行处理（默认已启用）
2. 减少 `max_revision_iterations` 限制修复次数
3. 使用更快的模型（如 `gpt-3.5-turbo`）

## 🎓 最佳实践

### 1. 主题描述要具体

```python
# ❌ 不好
topic = "物理"

# ✅ 好
topic = "经典力学：牛顿三定律和运动学"
```

### 2. 合理设置水平

```python
# ✅ 好
user_level = "beginner"
target_level = "intermediate"

# ⚠️ 跨度太大可能生成很多节点
user_level = "beginner"
target_level = "expert"
```

### 3. 查看执行日志

```python
# 始终查看 messages 了解系统做了什么
for msg in result["messages"]:
    print(msg)
```

## 🧹 清理缓存

使用清理脚本删除 Python 缓存文件：

```bash
python cleanup.py
```

## 📄 许可证

MIT License

## 🙏 致谢

本项目灵感来源于元认知学习理论和多智能体协作系统。

---

**Made with ❤️ using LangChain, Concurrent Processing, and DeepSeek**

